# k8s/base/secrets.yaml - Application Secrets
# NOTE: In production, use sealed-secrets or external-secrets-operator
# These are placeholders - override in overlays or use GitOps secret management
apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  labels:
    app: mern-demo
type: Opaque
stringData:
  # IMPORTANT: Replace these in overlay-specific secrets or via external secret management
  mongo-uri: "mongodb://mongo:27017/mern_demo"
  jwt-secret: "change-me-in-production-use-external-secrets"
